# Legacy source archive

These files are preserved from the previous jemdoc site. They are historical
reference material and include outdated affiliation/template information.
They are not inputs to the current site. Edit `content/*.json` and run
`python3 scripts/build.py` from the repository root instead.
